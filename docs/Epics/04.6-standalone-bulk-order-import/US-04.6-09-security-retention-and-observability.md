# US-04.6-09 — Basic security and operational safety

- **Epic:** [E04.6 — Standalone Bulk Order Import (CSV / XLSX)](README.md)
- **Delivery rank:** 9 of 10
- **Priority:** P0
- **Horizon:** NEXT
- **Story type:** Operations
- **Status:** Implemented locally — 2026-09-22
- **Dependencies:** [US-04.6-08](US-04.6-08-import-history-and-results-export.md)

## User story and value

As an Akeed operator responsible for merchant and customer data, I want bulk import to be tenant-isolated, safe to accept uploads for, free of customer PII in logs, and bounded in how long it retains raw imported data, so that a feature touching thousands of phone numbers at once can be exposed to real merchants without inventing a new incident.

**Business value:** This is the minimum bar to let real merchants use bulk import — not an operations platform. Anything beyond stopping tenant leaks, unsafe uploads, PII logging and indefinite data retention is deferred until real usage justifies building it.

## Scope

- A tenant-isolation test matrix across every order-import endpoint.
- A hardening review — plus tests for any gap found — of upload handling already built by US-04.6-02.
- A focused PII-log test across the full upload → commit → start → release flow.
- One daily retention/purge job for committed batches' raw row data.
- Confirming the `STANDALONE_BULK_IMPORT_ENABLED` kill switch covers every mutation endpoint and hides every entry point.

**Out of scope (see [Deferred / Post-MVP](#deferred--post-mvp)):** a metrics dashboard, alerting infrastructure, a staff read-only import UI, a staff pause/resume workflow, an admin import-access audit system, a detailed runbook, malware scanning, DLP, mapping-profile cleanup, production SLA/performance monitoring.

## Acceptance criteria

1. **Tenant isolation — MUST HAVE.**
   - Every order-import endpoint resolves `org_id` and the writable source from the authenticated session, reusing `StandaloneSourceResolver` — no new resolver or authorization framework.
   - A batch or row lookup filters by `org_id`; another organization's batch id returns 404 `IMPORT_BATCH_NOT_FOUND`, never 403, matching the existing pattern for orders.
   - Queries run under the same RLS policy as `orders` (`org_id = get_user_org_id()`), reused as-is.
   - A test matrix covers {owner, admin, viewer, another org's owner, an unauthenticated caller, a Shopify org, the flag off} × every order-import endpoint (upload, mapping, commit, start-quote, start, stop, resume, discard, the batch/rows reads, and the Verifications `importBatchId` filter). Mutation endpoints reject viewers with the existing role-check pattern.
2. **Basic upload hardening — MUST HAVE.**
   - Confirm — and close any gap found in — what US-04.6-02 already built: memory-only storage with the buffer released after parsing, no filesystem writes, the file name never used as a path, limits enforced before parsing.
   - The parser time cap (20 s) actually aborts pathological input with `IMPORT_FILE_UNREADABLE`; add the test if US-04.6-02 didn't include one.
   - The XLSX reader ignores external workbook links, macros (`vbaProject.bin`) and embedded objects — confirmed with a fixture containing an external link, not only a macro fixture.
   - Cell values are only ever read as data, never evaluated as formulas — already the parser's design; add a regression test if one is missing.
   - The raw request body and uploaded file are excluded from error-reporting and logging middleware.
   - No malware scanning, DLP or antivirus infrastructure is added. Uploaded files are never stored or served back — that remains the mitigation.
3. **PII-safe logging — MUST HAVE.**
   - Order-import code logs only through `buildBackendLog` with batch ID, org ID, status, counts, error codes and durations.
   - Phone numbers, customer names, addresses, raw cell values, raw uploaded files and full order payloads are never logged — building on US-04.6-02's existing `REDACTED_KEYS` additions (`filename`, `file_name`, `originalname`, `cells`, `raw`).
   - One new focused test runs upload → commit → start → release with a fixture containing unique marker phone numbers, names and addresses, captures all logger output, and asserts none of the markers (or the file name) appear anywhere in it. This is the one new PII test this story requires; it covers the commit/start/release stages that US-04.6-02's existing log test didn't reach.
4. **Basic retention — KEEP IT SIMPLE.**
   - Reuse what already exists: the 24-hour draft expiry and row purge (US-04.6-02) and the 72-hour start-window expiry (US-04.6-07). Neither changes.
   - Add exactly one new job: a daily `import.purge` job that, for committed batches older than 90 days, sets `raw = NULL` and `normalized = NULL` on their rows while keeping outcome, issues, order ID and row number. Batched (1,000 rows per statement) and idempotent (safe to re-run).
   - No retention reporting, no second retention subsystem, and no mapping-profile cleanup in this story.
5. **Feature flag / kill switch — KEEP.**
   - `STANDALONE_BULK_IMPORT_ENABLED=false` hides every import entry point (the "New import" action, the batch-status routes) and blocks upload, mapping, commit, start and resume with `IMPORT_DISABLED`.
   - Stop, and the existing Verifications filter for already-imported orders, keep working.
   - Releasing batches continue exactly per the existing US-04.6-07 behavior; this story adds no new pause mechanism.
   - A test confirms the flag is checked on every mutation endpoint (not only the upload guard) and that flipping it repeatedly leaves no batch in an inconsistent state.

## Edge cases

- Brute-force enumeration of batch IDs (UUIDs; 404; the existing per-user upload throttle from US-04.6-02 is the only rate limiting required here).
- A viewer calling any mutation endpoint.
- A session from a since-removed membership.
- An upload body containing a single 5 MB line with no delimiter — must not hang the parser past the 20 s cap.
- An XLSX with an external workbook link.
- The flag being flipped on and off repeatedly mid-batch.
- The daily purge job running twice in a row on the same day (idempotent, no double-null errors).

## Implementation notes

- **Backend:**
  - No new authorization framework — reuse `StandaloneSourceResolver` and the existing RLS pattern.
  - The purge job runs on the existing `order-import` BullMQ queue as a repeatable job, alongside `import.expire` (US-04.6-07).
  - Extend `REDACTED_KEYS` only if the PII-log test finds a gap; don't pre-emptively add fields that aren't logged.
- **Frontend:** No new UI. The existing "disabled by flag" and auto-pause states already built by US-04.6-07 are reused as-is.
- **Data:** No new columns. Add an index only if the 90-day purge query genuinely needs one (`committed_at` on the batch) — nothing else.

## Test requirements

- The authorization matrix described in AC1.
- The upload-hardening gap tests described in AC2 (parser timeout, external-link XLSX).
- The PII-log scan described in AC3.
- Purge-job idempotency and the 90-day boundary (day 89 vs. day 91).
- Kill-switch coverage per mutation endpoint.
- Satisfy the applicable [shared Definition of Done](../README.md); record test results during implementation, not when this backlog is authored.

## Migration and rollout

An additive index on the batch's `committed_at` (or on `order_import_rows` if the purge scan needs it there instead) only if the purge query requires one; otherwise none. Enable the purge job before the flag is turned on for any pilot organization.

## Deferred / Post-MVP

These were in scope in an earlier draft of this story and are explicitly deferred until bulk import has real usage to observe:

- A metrics dashboard and the underlying metrics architecture (`order_import.upload/validate/commit/release` counters, the funnel view).
- Alerts: a 10-minute release-stall alert, a commit-stuck alert, an upload-failure-rate alert, a purge-failure alert, and the alerting infrastructure behind them.
- A full operational dashboard, or a staff-facing import dashboard.
- A staff read-only batch UI on the admin store page, and the staff pause/resume workflow (with its history and precedence rules).
- An admin import-access audit system beyond the existing `admin_access_audit` table (unused by this story — there's no staff feature yet to audit).
- A detailed runbook (`docs/runbooks/bulk-order-import.md`) — write it once there's an actual incident shape to document, or earlier if US-04.6-10's pilot needs it sooner.
- 365-day mapping-profile cleanup (not required by any existing infrastructure today).
- Enterprise DLP or malware-scanning infrastructure.
- Extensive operational edge-case suites and production SLA/performance monitoring.

If the pilot in US-04.6-10 surfaces an operational gap (a stuck job nobody notices, a support request nobody can answer), address it as a small, targeted follow-up rather than building the full list above speculatively.

## Evidence and references

**VERIFIED FROM CODE:**
- `admin_access_audit` in [`schema.ts`](../../../src/infrastructure/database/schema.ts) (not used by this MVP story; kept for a future staff feature).
- [`backend-log.util.ts`](../../../src/shared/logging/backend-log.util.ts) and its `REDACTED_KEYS`, already extended by US-04.6-02 for `filename`, `file_name`, `originalname`, `cells` and `raw`.
- RLS pattern `org_id = get_user_org_id()` on `orders`, reused unchanged.
- US-04.6-02's memory-storage upload, macro rejection and filename sanitization; US-04.6-02's 24 h draft/row expiry; US-04.6-07's 72 h start-window expiry job (`import.expire`) and its `STANDALONE_BULK_IMPORT_ENABLED` gate.

**IMPLEMENTED LOCALLY — 2026-09-22:**

*Conflicts found in code, and the decisions taken (confirmed with the product owner before coding):*
- **The kill switch also blocked stop.** `OrderImportAccessGuard` threw `IMPORT_DISABLED` on every route, stop included, although AC5 and the epic README say stop keeps working. US-04.6-07 explicitly left this to this story. Fixed with a per-route `whenDisabled: 'allow'` option on the existing `OrderImportAccess` decorator (no new framework). Only `POST /:id/stop` uses it; role, source, onboarding and org checks still apply.
- **The "existing" 24 h draft purge did not exist.** AC4 says to reuse it, but US-04.6-02 had handed it to this story (its Implementation notes: "A daily purge job (US-04.6-09) deletes expired drafts"), and no purge code existed. Expired drafts therefore kept their raw customer rows forever. `import.purge` now deletes expired drafts as well. It is still one job and one retention path.
- **`order_import_rows.raw` was `NOT NULL`.** It is set in 0037, so it could not be set to NULL. Migration `0040` relaxes it. This is backward compatible, and every writer still sets it.
- **Known limitation: stop is API-only while the flag is off.** AC5 also hides the batch-status page while the flag is off, and this story adds no UI, so the Stop button can't be reached then. The endpoint answers `200`.

*Files changed:*
- `src/modules/order-imports/guards/order-import-access.guard.ts`: `OrderImportAccessOptions` / `whenDisabled`.
- `src/modules/order-imports/order-imports.controller.ts`: stop is exempt from the kill switch.
- `src/modules/order-imports/order-import-queue.constants.ts`: `import.purge` job, scheduler id, daily interval, `ORDER_IMPORT_RETENTION_DAYS = 90`.
- `src/modules/order-imports/release/order-import-purge.service.ts` (new): `run(now)` drains `deleteExpiredDrafts` and then `purgeCommittedRows` in 1,000-row statements. It logs counts and duration only.
- `src/modules/order-imports/release/order-import-release.scheduler.ts`: registers the daily `import-purge` scheduler on boot, next to `import-expire`, with the same `upsertJobScheduler` pattern.
- `src/modules/order-imports/order-import.processor.ts`, `order-imports.module.ts`: route and provide the job.
- `src/infrastructure/database/repositories/order-imports.repository.ts`: `deleteExpiredDrafts(now, limit)` and `purgeCommittedRows(cutoff, limit)`.
  - The purge covers batches with `committed_at < cutoff`, and failed commits (`committed_at` is never set) by `updated_at < cutoff`. It selects only rows where `raw` or `normalized` is still set, so it is idempotent.
  - Outcome, issues, `order_id` and `row_number` are not touched.
- `src/infrastructure/database/schema.ts`: `order_import_rows.raw` is nullable. The readers already treated a missing `raw` as `{}` (`order-import-detail.service.ts` `stringRecord`, `order-import-rows.service.ts:166`, repository mappers), so a purged row shows empty cells.
- `drizzle/0040_order_import_row_retention.sql` and `drizzle/meta/_journal.json`: `ALTER COLUMN raw DROP NOT NULL`. Hand-written, re-runnable (applied twice by the contract suite).
- **No index added.** The purge scans `order_import_batches`, a handful of rows per org per day, and reaches rows through `idx_order_import_rows_batch_outcome`, whose leading column is `batch_id`.
- `scripts/order-import-fixtures/build-fixtures.ts`: new fixture `external-link.xlsx`. Regenerated with `npm run fixtures:order-imports`. Only the new file and `index.json` changed; every other fixture is byte-identical.
- Tests:
  - New: `order-imports.authorization.http.spec.ts`, `release/order-import-purge.service.spec.ts`.
  - Extended: `order-import.processor.spec.ts`, `parsers/import-file-parser.spec.ts`, `parsers/parse-import-file.fixtures.spec.ts`, `test/order-imports.contract-spec.ts`.
- No frontend change. The flag-off behaviour already exists in `useBulkImportAvailability.ts` (the "New import" action) and `OrderImportPagesStandalone.tsx` (the batch pages show the disabled state on `IMPORT_DISABLED`).

*AC1: authorization matrix* (`order-imports.authorization.http.spec.ts`, 115 tests)
- The harness uses the real `DualAuthGuard` in front of a token table, and the real controller, `OrderImportAccessGuard`, `StandaloneSourceResolver`, upload throttle, multer options, worker parser and services, the release service included.
- Only the repositories are faked. They are org-scoped and return organization A's batch only for A's id and source.
- There are 13 routes × 8 callers, each asserting status and code. Every refusal before the handler also asserts that nothing of organization A was read or written.

| Caller | Batch routes (`/:id…`) | Upload | Template / open-drafts list |
|---|---|---|---|
| owner, admin (org A) | success (200 / 202 / 204) | 201 | 200 |
| viewer (org A) | reads 200; mapping, row PATCH, commit, start-quote, start, stop, resume and DELETE → 403 `IMPORT_ROLE_REQUIRED` | 403 `IMPORT_ROLE_REQUIRED` | 200 |
| owner of org B | 404 `IMPORT_BATCH_NOT_FOUND` (never 403) | 201, into org B only | 200, own data only |
| Shopify org owner | writes 403 `IMPORT_SOURCE_UNSUPPORTED`; reads 404 | 403 `IMPORT_SOURCE_UNSUPPORTED` | 200 |
| unauthenticated | 401 | 401 | 401 |
| removed membership (`ORGANIZATION_REQUIRED`, as `token-validator.service.ts:351` throws) | 403 | 403 | 403 |
| flag off | 403 `IMPORT_DISABLED`, **except stop → 200** | 403 `IMPORT_DISABLED` | 403 `IMPORT_DISABLED` |

- The Verifications `importBatchId` filter answers 200 to members of every org, and to the flag-off caller, with the session `orgId` passed to `listByOrg`. It answers 401 unauthenticated and 403 to a removed member. Repository scoping by `org_id` is `verifications.repository.ts:312-314`.
- A malformed id is 404 through `batchIdPipe` (existing). RLS `org_id = get_user_org_id()` is reused unchanged and was already proven by the contract test "limits a signed-in member to their own organization through RLS".
- Mutation check: removing the stop exemption makes `POST /:id/stop as flagOff` and the flip test fail (2 failures). Restored.
- Enumeration: batch ids are UUIDs, and another org's id is indistinguishable from an unknown one (404). The existing per-user upload throttle (10/min) is unchanged.

*AC2: upload hardening review*

| Item | Where | Verdict |
|---|---|---|
| Memory-only storage, no filesystem writes | `order-imports.module.ts:41` `orderImportMulterOptions`: no `dest`/`storage`; `fileSize`, `files: 1`, `fields`, `parts` limits | OK, already built |
| Limits before parsing; denial before reading the body | the guard runs before `OrderImportUploadInterceptor`; multer stops at `fileSize` | OK (existing HTTP tests) |
| File name never used as a path | `order-imports.service.ts:102` `sanitizeImportFileName`; stored as data only; `filename`/`originalname` redacted in logs | OK |
| Buffer released | the buffer is held only by the request. `parseInWorker` copies it to the worker, and nothing module-level keeps a reference, so it is collected with the request | OK |
| Parser time cap 20 s actually aborts | `import-file-parser.ts:66-72`: `worker.terminate()` on timeout → `IMPORT_FILE_UNREADABLE`/`parse_timeout` | OK. **Gap test added** for pathological input: a single 5 MB line with no delimiter, and a quote left open over 5 MB. Under the real 20 s cap they settle in about 0.37 s and 0.45 s. With a spent budget the worker is killed within about 31 ms with `IMPORT_FILE_UNREADABLE`. |
| Macros | `file-sniffer.ts:75` rejects any `vbaProject.bin` | OK (`macro.xlsm` fixture) |
| External workbook links and embedded objects | `xlsx-grid.parser.ts:84-92` (`cellFormula:false`, `bookVBA:false`, cached values only) | **Gap closed.** New `external-link.xlsx` has an `externalLink` part to `file:///C:/secret/prices.xlsx`, a formula `[1]Prices!A1` and an embedded `oleObject1.bin`. It parses to the formula's cached value (42, not the linked book's 999), and a spy proves no `fs` read, open or stat happens during the parse. |
| Cells never evaluated as formulas | `formulas.xlsx` fixture (a stale cache stays stale) | OK, regression test already existed |
| Zip bomb | `zip-guard.ts:32` counts inflated bytes | OK (`zip-bomb.xlsx`) |
| Request body and file excluded from error and log middleware | `main.ts:16`: `rawBody` covers JSON/urlencoded only, not multipart. `GlobalExceptionFilter` logs no request. There is no request-logging middleware. The worker sends no library message back (`parse-import-file.worker.ts:34`) | OK, and proven end to end by the AC3 scan |
| Malware scanning, DLP, AV | none | not added, by design; files are never stored or served back |

*AC3: PII-safe logging* (`test/order-imports.contract-spec.ts` › "logs no phone, name, address or file name from upload to release")
- Real PostgreSQL. The real `OrderImportsService.upload` (worker parser) → `OrderImportMappingService.save` (row validation) → `OrderImportCommitService.commit` → `OrderImportCommitProcessor.process` (acceptMany into held orders) → `OrderImportReleaseService.quote` + `start` → `OrderImportReleaseTickService.tick` until `completed`.
- Only the messaging port (the real dispatch claim) and the credit gate are fakes.
- Capture: `Logger.overrideLogger`, `process.stdout/stderr.write` and every `console.*` level.
- The scan asserts the output contains the flow's own actions (`order-import-upload`, `order-import-start`). It also asserts that none of these markers appear:
  - five phone forms (national, E.164, digits-only);
  - two name tokens;
  - four address and city tokens;
  - the file name `pii-marker-طلبات-Q7.csv` and the `pii-marker` prefix.
- Mutation check: temporarily logging the sanitized file name under an unredacted key makes the test fail, listing the file name. Restored.
- No `REDACTED_KEYS` change was needed.

*AC4: retention* (`import.purge`, daily)
- Contract tests pass:
  - Day 89 keeps `raw`/`normalized`. Day 91 clears both and keeps outcome, issues, `order_id` and row number exactly; the orders themselves are untouched.
  - A second run the same day returns `{draftsDeleted: 0, rowsPurged: 0}` without error.
  - 2,345 rows are cleared through several 1,000-row statements.
  - A failed commit is purged 90 days after its last update.
  - An expired draft is deleted with its rows, while a live draft and a recent committed batch stay.
- The existing 72 h `import.expire` is unchanged. No retention reporting and no mapping-profile cleanup.
- Rollout: the purge scheduler registers on worker boot, so it is enabled before the flag is turned on for any pilot org.

*AC5: kill switch*
- The matrix's flag-off column covers each of the 13 routes. Upload, mapping, commit, start and resume (and every other route but stop) answer `IMPORT_DISABLED`.
- The flip test runs on a paused batch:
  - off: resume is refused and the state is unchanged;
  - on: resume goes to releasing;
  - off: resume is refused, and stop answers 200 → stopped;
  - on: a repeated stop answers the same body, resume conflicts with `stopped`.
  - No state disagrees with its routes.
- The release tick and `import.expire` never read the flag (`order-import-release-tick.service.ts`, `order-import-expire.service.ts`), so releasing batches finish per US-04.6-07. The Verifications filter doesn't depend on the flag (see AC1).

*Commands and results (2026-09-22):*
- `npx tsc --noEmit -p tsconfig.json`: 0 errors.
- `npx eslint <touched files>`: 0 problems. `npm run log:check`: 0 violations. `npx prettier --write --end-of-line crlf|lf <touched files>`: each file keeps its existing line endings.
- `npx jest`: 134 suites, **3416 passed**.
  - New or extended specs: authorization matrix 115; purge service 2; processor 2; import-file-parser 8; fixtures 32.
- Contract suites, on disposable Docker Postgres (`postgres:17-alpine`, `E01_TEST_DATABASE_URL`):
  - `npm run test:contract:order-imports`: **45 passed** (39 before this story, plus 5 purge tests and 1 PII scan).
  - `test:contract:manual-orders`: 15 passed.
  - `test:contract:entitlements`: 7 passed.
  - `test:contract:shopify`: 9 passed.
  - `scripts/test-e045-contracts.ps1`: exit 0 (40 + 10 + 17 + 29 + 38 + 31 passed).
- No manual-order code was touched. `test:contract:manual-orders` passed after the change; no extraction was done, so the before/after rule does not apply.
- Duplicate grep over the new and changed files (currency list, `/^[A-Za-z0-9._:-]+$/`, the totalPrice regex, `findActiveByOrg`, `resolveDenial`, `hasAvailableSlot`, `PhoneNumberUtil`): no hits in production code. `findActiveByOrg` appears only as the fake integrations repository in the new matrix spec, feeding the real `StandaloneSourceResolver`.
- Migrations were run only against disposable containers, never a shared database.

*Not built (Deferred / Post-MVP, unchanged):* metrics or alerts, a staff pause/resume workflow, an admin batch panel, an admin audit system, and `docs/runbooks/bulk-order-import.md`.
