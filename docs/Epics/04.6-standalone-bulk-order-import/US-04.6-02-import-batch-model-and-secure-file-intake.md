# US-04.6-02 — Accept import files securely and persist parsed rows

- **Epic:** [E04.6 — Standalone Bulk Order Import (CSV / XLSX)](README.md)
- **Delivery rank:** 2 of 10
- **Priority:** P0
- **Horizon:** NEXT
- **Story type:** Feature
- **Status:** Implemented locally — 2026-09-19
- **Dependencies:** [US-04.6-01](US-04.6-01-source-neutral-order-hold-and-release.md)

## User story and value

As a Standalone owner or admin, I want to upload the CSV or Excel file I already have, so that I don't have to reformat or re-save it before Akeed can read my orders.

**Business value:** This removes the main support burden of spreadsheet import (encoding, delimiter and Excel quirks) and creates the durable batch that every later step reads from.

## Scope

- The batch/row/profile schema.
- The multipart upload endpoint.
- Type, size and structure limits.
- Encoding and delimiter detection.
- CSV and XLSX parsing into a uniform grid.
- Header normalization.
- The file-hash warning.
- Persisting raw rows.
- Template downloads.
- The draft cap and discard.

**Out of scope:** Field mapping logic (US-04.6-03), validation (US-04.6-04), object storage and streaming uploads over 5 MB.

## Acceptance criteria

1. `POST /api/order-imports` accepts one multipart `file` field from an owner/admin of a Standalone org with completed onboarding and the flag on. Otherwise it returns `IMPORT_DISABLED`, `IMPORT_ROLE_REQUIRED`, `IMPORT_SOURCE_UNSUPPORTED` or `IMPORT_SETUP_INCOMPLETE`, before reading the body where possible.
   - The role, single-active-source, Standalone-only and onboarding checks are **extracted** from `OrdersService.createManualOrder` into `StandaloneSourceResolver.resolveWritable(user, codeMap)`. `createManualOrder` then calls it with its existing `MANUAL_ORDER_*` codes, with unchanged status codes and bodies.
   - The import calls the same resolver with the `IMPORT_*` code map. No other code in this epic re-derives the source.
2. Files over 5 MB are rejected by the upload limiter with `IMPORT_FILE_TOO_LARGE`, without buffering the whole body. A missing file returns `IMPORT_FILE_REQUIRED`.
3. The format is decided by content, not the extension or client MIME type:
   - ZIP magic with an `xl/workbook.xml` part is XLSX.
   - An OLE2/CFB header means legacy `.xls` or a password-protected XLSX. These return `IMPORT_FILE_TYPE_UNSUPPORTED` or `IMPORT_FILE_PROTECTED` respectively.
   - A workbook with `vbaProject.bin` (macros) is rejected.
   - Other binary content returns `IMPORT_FILE_UNREADABLE`.
   - Otherwise the file is text.
4. Text decoding order:
   1. The BOM (UTF-8, UTF-16 LE/BE).
   2. Strict UTF-8.
   3. Windows-1256.

   The detected encoding is stored. The delimiter is chosen among `,`, `;` and tab by the most consistent column count over the first 50 non-empty lines; a tie prefers `,`. RFC 4180 quoting is honored, including multi-line cells. A malformed quote produces a row-level parse issue on that row only.
5. XLSX reads the first **visible** worksheet. Cells use their cached value, and formulas are never evaluated. Dates become ISO strings. Numbers keep the displayed text where the cell has a text or custom format, so phone numbers are not reformatted. Merged ranges keep the value in their top-left cell only. The response lists ignored sheet names.
6. Zip-bomb and size guards: total uncompressed size over 50 MB, or more than 5,000 non-empty data rows, or more than 100 columns, aborts with `IMPORT_FILE_UNREADABLE`, `IMPORT_ROW_LIMIT_EXCEEDED` or `IMPORT_COLUMN_LIMIT_EXCEEDED`. Nothing is persisted for an aborted upload.
7. Header handling:
   - The first non-empty row is the header. Headers are trimmed and NFC-normalized.
   - Blank headers become `Column N`. Duplicates get ` (2)`, ` (3)` suffixes.
   - Fully blank data rows are dropped and not counted.
   - Cells are trimmed. Values over 1,000 characters are truncated and flagged `FIELD_TOO_LONG`.
   - Rows with more cells than headers keep the extras under `Column N`. Missing cells are empty.
   - A file with a header but no data rows returns `IMPORT_FILE_EMPTY`.
8. On success, one transaction creates `order_import_batches` in `draft` (expires in 24 h) and inserts all rows in chunks of 500 as `{rowNumber (1-based spreadsheet row), raw: {header: value}}`. It returns `{batchId, status, fileName, format, encoding, delimiter, sheetName, ignoredSheets, headers, rowCount, sampleRows (first 5), duplicateFileOf?}`.
9. If a batch with the same SHA-256 exists for the org in the last 24 h (any status except `expired`), the response includes `duplicateFileOf: {batchId, createdAt, status}`. The client shows a warning; this is not an error.
10. More than 3 open drafts returns `IMPORT_TOO_MANY_DRAFTS`, listing the drafts to resume or discard. `DELETE /api/order-imports/:id` discards a `draft` (rows purged). Any other status returns `IMPORT_BATCH_STATE_CONFLICT`.
11. `GET /api/order-imports/template?format=csv|xlsx&locale=ar|en` returns a sample file with headers `order_id, customer_name, phone, amount, currency, payment_method, order_date, city, address, notes` (Arabic header labels for `ar`) and two Egyptian example rows. The CSV is UTF-8 with BOM.
12. The file name is stored sanitized: path segments stripped, control and bidi-override characters removed, at most 150 characters. It is never used as a filesystem path.

## Data contract

```text
order_import_batches
  id uuid PK, org_id uuid FK, integration_id uuid FK, created_by uuid,
  status text CHECK IN ('draft','committing','awaiting_start','releasing','paused',
                        'completed','stopped','not_started','expired','failed'),
  file_name text, file_sha256 char(64), file_size int, file_format text ('csv'|'xlsx'),
  encoding text NULL, delimiter text NULL, sheet_name text NULL,
  headers jsonb, row_count int,
  mapping jsonb NULL, options jsonb NULL, mapping_profile_id uuid NULL,
  counts jsonb NOT NULL DEFAULT '{}',          -- ready/invalid/duplicate/excluded/imported/released/withdrawn
  order_date_min date NULL, order_date_max date NULL,
  commit_idempotency_key text NULL, start_idempotency_key text NULL,
  committed_at, attested_by uuid, attested_at, attestation_version text,
  started_at, paused_reason text NULL, completed_at, expires_at, start_deadline_at,
  short_code text NOT NULL,                     -- 6-char base32, for IMP-<code>-<row>
  created_at, updated_at
  UNIQUE (org_id, commit_idempotency_key), UNIQUE (org_id, short_code)
  INDEX (org_id, created_at DESC), INDEX (org_id, file_sha256, created_at)

order_import_rows
  id uuid PK, batch_id uuid FK ON DELETE CASCADE, org_id uuid,
  row_number int, raw jsonb, normalized jsonb NULL,
  outcome text NULL CHECK IN ('ready','invalid','duplicate','excluded','imported'),
  issues jsonb NOT NULL DEFAULT '[]',          -- [{code, field?, params?}]
  include_override boolean NOT NULL DEFAULT false,
  dedupe_key text NULL, collapsed_into int NULL,
  order_id uuid NULL, webhook_event_id uuid NULL
  UNIQUE (batch_id, row_number), INDEX (batch_id, outcome, row_number)

order_import_mapping_profiles
  id uuid PK, org_id uuid, header_signature char(64), mapping jsonb, options jsonb,
  updated_by uuid, updated_at
  UNIQUE (org_id, header_signature)
```

Row-level security matches `orders`: `org_id = get_user_org_id()`.

## Edge cases

- A `.csv` extension on an XLSX file is parsed as XLSX. A `.xlsx` extension on CSV text is parsed as CSV.
- UTF-16 LE tab-separated "Unicode text" saved by Arabic Excel parses to correct Arabic.
- A Windows-1256 CSV decodes Arabic correctly and records `encoding = windows-1256`.
- A hidden first sheet is skipped in favor of the first visible one.
- A sheet with formatting applied to 1,048,576 rows but data in 40 counts 40 rows.
- Phone cells formatted as numbers: if the stored value is numeric, the raw numeric string is kept and US-04.6-04 flags scientific notation or the missing leading zero.
- Excel serial dates, including the 1900 leap-year bug, convert to ISO dates.
- An interrupted upload leaves no batch.
- Two identical uploads 1 second apart: both succeed as separate drafts, and the second carries `duplicateFileOf`.

## Implementation notes

- **Backend:**
  - New `src/modules/order-imports/` module: controller, service, `parsers/{file-sniffer,text-decoder,csv-grid.parser,xlsx-grid.parser}.ts` producing `Grid = {headers: string[], rows: {rowNumber, cells: string[]}[]}`, and repository.
  - Use `FileInterceptor` with memory storage and `limits: {fileSize, files: 1, fields: 5}`. Parse with a streaming CSV parser. Choose an XLSX reader per the epic's dependency note, and enforce the uncompressed-size cap while inflating.
  - Run parsing off the request's event-loop hot path if a 5,000 × 100 file exceeds 1 s (worker thread acceptable).
  - `@Throttle` 10 per minute per user.
- **Frontend:** No UI in this story beyond typed API functions. `fetchWithAuth` must allow a caller to omit `Content-Type` for `FormData`, so the browser sets the multipart boundary.
- **Data:** Additive migration for the three tables. A daily purge job (US-04.6-09) deletes expired drafts.
- **Operations:** Log the batch ID, format, encoding, row count and duration. Never log cell values or the file name.

## Test requirements

- Fixture tests for each encoding, delimiter and XLSX quirk listed above. Tests for limit boundaries: exactly 5,000 rows, 5,001 rows, 100 and 101 columns, and a 5 MB + 1 byte file.
- Tests for protected, macro, legacy `.xls`, PDF-renamed and zip-bomb files; role, flag and Shopify denial; the draft cap; and discard.
- `StandaloneSourceResolver` unit tests for both code maps. The manual-order contract suite passes unchanged after the extraction.
- Satisfy the applicable [shared Definition of Done](../README.md); record test results during implementation, not when this backlog is authored.

## Migration and rollout

The migration is additive, and the endpoint is gated by `STANDALONE_BULK_IMPORT_ENABLED`. Rollback: disable the flag. The tables can remain.

## Evidence and references

**VERIFIED FROM CODE:** No existing upload, multer, CSV or XLSX usage in `src/`. Throttling is available via `@nestjs/throttler` (see [`billing.controller.ts`](../../../src/modules/billing/billing.controller.ts)).

**ASSUMPTION / REQUIRES VALIDATION:** The XLSX library choice and its advisory status, and parse-time p95 for 5,000 × 100 on production hardware.

**IMPLEMENTED LOCALLY — 2026-09-19:**

Branch `feature/us-04.6-02-import-intake` off `develop`, six commits: `9537a39` deps, `2094d65` resolver extraction, `557922d` config, `ab1cd64` schema, `24a0282` parsers, `560027a` endpoints.

*Dependencies and advisory check.*
- The XLSX reader is SheetJS `xlsx@0.20.3`, installed from `https://cdn.sheetjs.com/xlsx-0.20.3/xlsx-0.20.3.tgz` and pinned by its sha512 integrity in `package-lock.json`. The npm `xlsx@0.18.5` package is not used.
  - `npm audit` cannot see a CDN tarball, so its advisories are tracked by hand.
  - 0.20.3 includes the fixes for CVE-2023-30533 (prototype pollution) and CVE-2024-22363 (ReDoS).
- `exceljs@4.4.0` was the alternative. It was rejected because:
  - its last stable release was October 2023;
  - it carries a transitive moderate `uuid` advisory, GHSA-w5hq-g745-h8pq;
  - it offers no displayed text and has no 1900-bug date handling.
- Also added:
  - `fflate@0.8.3`: ZIP listing and the byte-counted inflate cap;
  - `iconv-lite@0.7.3`;
  - `@types/multer` (dev).
- `csv-parse@7.0.2` was approved but then removed; see the deviations below.
- The `multer` bundled with `@nestjs/platform-express@11.1.12` (2.0.2) had 8 high-severity DoS and limit-bypass advisories, including GHSA-xf7r-hgr6-v32p and GHSA-qvfw-j98x-7q72. This story makes that code reachable, so it is pinned to `multer@2.4.0` through `overrides`.
- `npm audit`: 37 findings before, 36 after (the multer entry is gone). None come from the new packages.

*Files.*
- **Resolver (AC1).** `src/modules/order-ingestion/standalone-source-resolver.ts` (with spec) holds `resolveWritable` and `assertWritableRole`, plus `MANUAL_ORDER_SOURCE_CODES` and `IMPORT_SOURCE_CODES`.
  - It is provided by `OrderIngestionModule` and called only through `StandaloneOrderIngestionService.resolveWritableSource` / `assertWritableRole`.
  - `OrdersService.createManualOrder` now calls those two methods in its original order: role, then the 400s, then the source. `integrationsRepo` is gone from `OrdersService`.
  - `ingestion-boundary.spec.ts` gains a static check: under `orders`, `order-imports` and `order-ingestion`, only the resolver calls `findActiveByOrg` or tests `platformType !== 'standalone'`.
- **Config.** `src/shared/config/bulk-import.config.ts` (with spec), wired into `validateEnv`, `.env.example` and `docs/ENVIRONMENT.md`:
  - `STANDALONE_BULK_IMPORT_ENABLED`
  - `BULK_IMPORT_MAX_ROWS`, `_MAX_COLUMNS`, `_MAX_OPEN_DRAFTS`, `_MAX_FILE_BYTES`, `_MAX_UNCOMPRESSED_BYTES`, `_PARSE_TIMEOUT_MS`

  The limits can be lowered but never raised past the story values.
- **Schema.** `drizzle/0037_order_import_batches.sql` plus its journal entry, and the three tables in `schema.ts`, exactly as the data contract. Beyond the contract:
  - a `(batch_id, org_id)` composite FK, so a row cannot claim another org than its batch;
  - `UNIQUE(id, org_id)` on batches;
  - a Crockford short-code CHECK;
  - RLS `org_id = get_user_org_id()` on all three tables.
- **Parsers** in `src/modules/order-imports/parsers/`, all pure:
  - `file-sniffer`, `zip-guard`, `text-decoder`, `csv-grid.parser`, `xlsx-grid.parser`, `header-normalizer`, `file-name`, `parse-import-file`;
  - `parse-import-file.worker` and `import-file-parser`: the worker thread, with a hard `terminate()` at the parse budget and 2 concurrent parses.

  `src/modules/order-imports/csv-writer.util.ts` is the shared writer that US-04.6-08 will reuse.
- **Module** `src/modules/order-imports/`:
  - controller, service, `order-imports.errors.ts` (the code → status table), DTO types, `order-import-template.ts` and `short-code.ts`;
  - guards: `OrderImportAccess('read'|'write')`, `OrderImportAccessGuard` and `@ImportSource()` are the reusable gate. `OrderImportUploadThrottleGuard` limits uploads per user;
  - `order-import-upload.interceptor.ts`;
  - `src/infrastructure/database/repositories/order-imports.repository.ts`, registered in `DatabaseModule`.
- **Logging.** `backend-log.util.ts` adds `filename`, `file_name`, `originalname`, `cells` and `raw` to `REDACTED_KEYS`. Upload logs carry only the batch id, format, encoding, row and column counts, duplicate flag and duration.
- **Fixtures and tests.**
  - `scripts/order-import-fixtures/build-fixtures.ts` and `scripts/generate-order-import-fixtures.ts` (`npm run fixtures:order-imports`) write 29 fixtures to `test/fixtures/order-imports/`, each with a hand-written `<file>.expected.json`.
  - `.gitattributes` marks those fixtures `-text`, so `autocrlf` never rewrites their deliberate line endings.
  - `test/order-imports.contract-spec.ts`, `test/jest-order-imports-contract.json`, `scripts/test-order-imports-contract.ps1` and `npm run test:contract:order-imports`.
- **Frontend** (`akeed-frontend`, not a git repository).
  - `src/shared/lib/auth.ts`: the new `buildRequestHeaders` and `isBrowserEncodedBody`. `FormData`, `Blob` and `URLSearchParams` bodies no longer get a forced `Content-Type`; every other request keeps `application/json` as before, including the embedded 401 retry.
  - `src/features/order-imports/api/orderImportsApi.ts` and `index.ts`:
    - `uploadOrderImport`, `discardOrderImport` and `downloadOrderImportTemplate` (returns a Blob; no token in the URL);
    - `orderImportErrorCodes` and `isOrderImportApiError`;
    - `drafts` read from `IMPORT_TOO_MANY_DRAFTS`.

*HTTP statuses chosen for the `IMPORT_*` codes.* The epic fixes only the codes.
- 403: `DISABLED`, `ROLE_REQUIRED`, and `SOURCE_UNSUPPORTED` for a non-Standalone source. `SOURCE_UNSUPPORTED` is 409 when there is no active source or more than one, following the manual-order statuses.
- 409: `SETUP_INCOMPLETE`, `TOO_MANY_DRAFTS` (the body carries `drafts[]`) and `BATCH_STATE_CONFLICT`.
- 400: `FILE_REQUIRED` and `VALIDATION_FAILED`.
- 413: `FILE_TOO_LARGE`.
- 415: `FILE_TYPE_UNSUPPORTED`.
- 422: `FILE_PROTECTED`, `FILE_UNREADABLE`, `FILE_EMPTY`, `ROW_LIMIT_EXCEEDED` and `COLUMN_LIMIT_EXCEEDED`.
- 429: `RATE_LIMITED`, with `Retry-After`.
- 404: `BATCH_NOT_FOUND`. A malformed id also answers 404.

*Commands and results.*
- **Baseline before the extraction.**
  - Orders and ingestion unit tests: 66/66.
  - `test:contract:manual-orders`: 15/15.
- **After the extraction.**
  - The same unit set: 80/80. The 14 new tests are resolver cases.
  - `test:contract:manual-orders`: 15/15, both after the extraction and again at the end.
- **Full `npx jest`:** 111 suites, 2281/2281 passing.
- **Order-import unit specs:** 10 suites, 153 tests:
  - the 29 fixtures, each against its expected grid;
  - the tokenizer, delimiter, header, decoder, sniffer, zip-guard, deadline, file-name and CSV-writer cases;
  - the worker;
  - the service;
  - 17 HTTP cases.
- **`test:contract:order-imports` (Docker Postgres 17):** 10/10.
  - The migration applies twice.
  - A 5,000-row draft is stored in chunks.
  - A failing row insert leaves no batch.
  - Duplicates are found within 24 h and never match the batch itself; expired batches, uploads older than 24 h and other orgs are excluded.
  - Six concurrent uploads under a cap of 3 give exactly 3 drafts.
  - Expired and non-draft batches are not counted against the cap.
  - Short codes are retried on collision, and generation gives up after 5 attempts.
  - Discard purges the rows, answers 404 for another org and conflicts for other states.
  - The CHECK and FK constraints hold.
  - RLS isolates organizations.
- **Other contract suites.**
  - `test:contract:shopify`: 9/9.
  - E04.5 contracts (`test:contract:e045`): 165/165.
  - `test:contract:entitlements`: 6/7. The failing case is "rejects a blocked source committed after the caller snapshot without reserving usage". **It fails identically on `develop` at `4053042`**, before this story, so it was not caused here.
- **Checks on touched files.** `npx tsc --noEmit -p tsconfig.json`, `npx eslint` and `npm run log:check` are clean. `npx prettier --end-of-line crlf` was applied.
- **Build.**
  - `npm run build` succeeds.
  - `dist/src/modules/order-imports/parsers/parse-import-file.worker.js` is emitted beside its caller.
  - `parseInWorker` was run from `dist` against `arabic-excel.xlsx`: it returns the sheet `الطلبات` with 2 rows.
  - Note: `dist` has had a `dist/src/...` layout since `scripts/shopify/*.ts` was added (b97f6ea, 2026-09-03). That is not caused by this story, and is flagged separately.
- **Frontend.**
  - `npx tsc --noEmit`, `npx eslint` on the touched files and `npm run build` all pass.
  - A throwaway script ran `buildRequestHeaders` against GET with no body, JSON strings, a caller-set `Content-Type`, FormData, Blob and URLSearchParams. All passed.
  - All existing `fetchWithAuth` callers send `JSON.stringify(...)` bodies or none, so their headers are unchanged.
  - No browser check was done: this story adds no UI.

*Parse timings* (local Windows laptop, Node 22.22; in-process, after one warm-up):

| File | Time |
| --- | --- |
| CSV 5,000 × 20 | 15–33 ms |
| XLSX 5,000 × 20 | 271–376 ms |
| CSV 5,000 × 100 | 51–68 ms |
| XLSX 5,000 × 100 | 1,375–1,459 ms |

The 5,000 × 20 budget of under 1 s is met for both formats. XLSX at 5,000 × 100 exceeds 1 s, so parsing runs in a worker thread, as the implementation notes require. The worker adds about 50 ms of startup when compiled.

*Deviations.*
- **csv-parse was replaced by an in-house RFC 4180 tokenizer** (`readCsvRecords`, about 80 lines, fully tested). Probing csv-parse 7.0.2 showed its recovery breaks the story's rule that a malformed quote affects only its own row:
  - with `skip_records_with_error`, an unclosed quote silently swallows the following rows;
  - `relax_quotes` accepts stray text without flagging it.

  The tokenizer flags `CSV_MALFORMED_QUOTE` on that row only and resumes on the next line. Quotes are special only at the start of a field, as in Excel. The package was uninstalled.
- **`CSV_MALFORMED_QUOTE` is a new row issue code.** It is added to the README contract list.
- **The migration was hand-written**, not produced by `db:generate`. `drizzle/meta` snapshots stop at 0015, so generation would re-emit 0016–0036. This follows the 0036 precedent.
- **Throttling:**
  - The global `ThrottlerGuard` keys by IP and runs before authentication, so the upload route uses `@SkipThrottle()`.
  - It is replaced by a guard that counts per user on the shared throttler storage and answers `IMPORT_RATE_LIMITED`.
  - The 10/min limit is a constant, not an environment setting.
- **Macro workbooks** (`vbaProject.bin`) and `.xlsb` files return `IMPORT_FILE_TYPE_UNSUPPORTED`; the story does not name a code for them. A ZIP without `xl/workbook.xml` returns `IMPORT_FILE_UNREADABLE`.
- **Blank columns.** A column with neither a header nor any value is dropped: a trailing delimiter, or data starting past column A. `Column N` keeps the spreadsheet position N.
- **Serial 60** becomes Excel's phantom `1900-02-29`, the date Excel displays. US-04.6-04 will reject it as an invalid date.
- **Not persisted:** `ignoredSheets` (the data contract has no column for it). It is returned in the upload response only.
- **Template defaults** are `format=csv` and `locale=en` when omitted. Any member may download the template while the flag is on.

*Duplicate check (shared rule 4).*
- `order-imports` contains none of the following, and no second CSV writer exists:
  - `findActiveByOrg`, `platformType`/`onboardingStatus` checks
  - `resolveDenial`, `hasAvailableSlot`
  - `PhoneNumberUtil`
  - the currency list
  - the idempotency-key regex
- Remaining hits in `orders.service.ts` are owned by later stories:
  - `resolveDenial`, `hasAvailableSlot` and `retryReadinessReason`'s onboarding check (US-04.6-07);
  - `/^[A-Za-z0-9._:-]+$/` (US-04.6-06).

*Known limitations.*
- Parse times are from a laptop; production p95 is still TARGET VALIDATION REQUIRED.
- UTF-16 is detected only with a BOM. Excel's "Unicode text" always writes one.
- The worker concurrency cap (2) is per process.
- The throttle uses in-memory throttler storage, so it is per instance, like every other throttle in the app.
